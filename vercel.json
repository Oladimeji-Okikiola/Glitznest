{
    "rewrites": [
      {
        "source": "/:path*",
        "destination": "/:path*.html"
      },
      {
        "source": "/categories/:categoryPath*",
        "destination": "/categories/:categoryPath*.html"
      }
    ]
  }